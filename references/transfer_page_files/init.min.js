/******/ (function(modules) { // webpackBootstrap
/******/ 	// install a JSONP callback for chunk loading
/******/ 	function webpackJsonpCallback(data) {
/******/ 		var chunkIds = data[0];
/******/ 		var moreModules = data[1];
/******/ 		var executeModules = data[2];
/******/
/******/ 		// add "moreModules" to the modules object,
/******/ 		// then flag all "chunkIds" as loaded and fire callback
/******/ 		var moduleId, chunkId, i = 0, resolves = [];
/******/ 		for(;i < chunkIds.length; i++) {
/******/ 			chunkId = chunkIds[i];
/******/ 			if(Object.prototype.hasOwnProperty.call(installedChunks, chunkId) && installedChunks[chunkId]) {
/******/ 				resolves.push(installedChunks[chunkId][0]);
/******/ 			}
/******/ 			installedChunks[chunkId] = 0;
/******/ 		}
/******/ 		for(moduleId in moreModules) {
/******/ 			if(Object.prototype.hasOwnProperty.call(moreModules, moduleId)) {
/******/ 				modules[moduleId] = moreModules[moduleId];
/******/ 			}
/******/ 		}
/******/ 		if(parentJsonpFunction) parentJsonpFunction(data);
/******/
/******/ 		while(resolves.length) {
/******/ 			resolves.shift()();
/******/ 		}
/******/
/******/ 		// add entry modules from loaded chunk to deferred list
/******/ 		deferredModules.push.apply(deferredModules, executeModules || []);
/******/
/******/ 		// run deferred modules when all chunks ready
/******/ 		return checkDeferredModules();
/******/ 	};
/******/ 	function checkDeferredModules() {
/******/ 		var result;
/******/ 		for(var i = 0; i < deferredModules.length; i++) {
/******/ 			var deferredModule = deferredModules[i];
/******/ 			var fulfilled = true;
/******/ 			for(var j = 1; j < deferredModule.length; j++) {
/******/ 				var depId = deferredModule[j];
/******/ 				if(installedChunks[depId] !== 0) fulfilled = false;
/******/ 			}
/******/ 			if(fulfilled) {
/******/ 				deferredModules.splice(i--, 1);
/******/ 				result = __webpack_require__(__webpack_require__.s = deferredModule[0]);
/******/ 			}
/******/ 		}
/******/
/******/ 		return result;
/******/ 	}
/******/
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// object to store loaded and loading chunks
/******/ 	// undefined = chunk not loaded, null = chunk preloaded/prefetched
/******/ 	// Promise = chunk loading, 0 = chunk loaded
/******/ 	var installedChunks = {
/******/ 		"init": 0
/******/ 	};
/******/
/******/ 	var deferredModules = [];
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	var jsonpArray = window["webpackJsonp"] = window["webpackJsonp"] || [];
/******/ 	var oldJsonpFunction = jsonpArray.push.bind(jsonpArray);
/******/ 	jsonpArray.push = webpackJsonpCallback;
/******/ 	jsonpArray = jsonpArray.slice();
/******/ 	for(var i = 0; i < jsonpArray.length; i++) webpackJsonpCallback(jsonpArray[i]);
/******/ 	var parentJsonpFunction = oldJsonpFunction;
/******/
/******/
/******/ 	// add entry module to deferred list
/******/ 	deferredModules.push(["./Scripts/init.ts","vendors~app~init"]);
/******/ 	// run deferred modules when ready
/******/ 	return checkDeferredModules();
/******/ })
/************************************************************************/
/******/ ({

/***/ "./Scripts/init.ts":
/*!*************************!*\
  !*** ./Scripts/init.ts ***!
  \*************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = __webpack_require__(/*! tslib */ "../node_modules/tslib/tslib.es6.js");
const axios_1 = tslib_1.__importDefault(__webpack_require__(/*! axios */ "../node_modules/axios/index.js"));
window.Alkami.AccountLinking = window.Alkami.AccountLinking || {};
window.Alkami.AccountLinking.installVuexPlugin = true;
window.Alkami.AccountLinking.features = {
    manageAggregation: false,
    manageInternal: false,
    manageCrossAccount: false,
    manageACH: true,
    manageDebitCard: true,
};
window.Alkami.AccountLinking.init = async (includeVue = true, includeIrisVue = true, installVuexPlugin = true) => {
    window.Alkami.AccountLinking.installVuexPlugin = installVuexPlugin;
    await window.Alkami.AccountLinking.injectCSS(includeIrisVue);
    await window.Alkami.AccountLinking.injectJS(includeVue, includeIrisVue);
};
window.Alkami.AccountLinking.injectCSS = async (includeIrisVue = true) => {
    const axiosInstance = axios_1.default.create({
        baseURL: '/AccountLinking/',
    });
    let isMobile = false;
    try {
        let pathSplit = window.location.pathname.split('/');
        pathSplit = pathSplit.filter((value) => value !== '');
        isMobile = pathSplit[0].toLowerCase() === 'mobile';
    }
    catch (error) {
        // Ignore error
    }
    const cssResponse = await axiosInstance.get(`GetCss?includeIrisVue=${includeIrisVue ? 'true' : 'false'}&isMobile=${isMobile ? 'true' : 'false'}`);
    // Axios auto follows redirects, but in the event they fix things we can short out here instead to only accept 2XX
    if (!cssResponse || cssResponse.status < 200 || cssResponse.status >= 300) {
        AccountLinkingUnavailable('css');
        return;
    }
    const cssToInject = cssResponse.data;
    if (cssToInject) {
        // Verify the payload is links to load
        if (!cssToInject.trim().toLowerCase().startsWith('<link ')) {
            AccountLinkingUnavailable('css');
            return;
        }
        const head = document.head;
        const tempElement = document.createElement('div');
        tempElement.innerHTML = cssToInject;
        while (tempElement.children.length > 0) {
            head.appendChild(tempElement.children[0]);
        }
    }
};
window.Alkami.AccountLinking.injectJS = async (includeVue = true, includeIrisVue = true) => {
    const axiosInstance = axios_1.default.create({
        baseURL: '/AccountLinking/',
    });
    const jsResponse = await axiosInstance.get(`GetJavaScript?includeVue=${includeVue ? 'true' : 'false'}&includeIrisVue=${includeIrisVue ? 'true' : 'false'}`);
    // Axios auto follows redirects, but in the event they fix things we can short out here instead to only accept 2XX
    if (!jsResponse || jsResponse.status < 200 || jsResponse.status >= 300) {
        AccountLinkingUnavailable('javascript');
        return;
    }
    const jsToInject = jsResponse.data;
    if (jsToInject) {
        // Verify the payload is scripts to load
        if (!jsToInject.trim().toLowerCase().startsWith('<script ')) {
            AccountLinkingUnavailable('javascript');
            return;
        }
        const body = document.body;
        const tempElement = document.createElement('div');
        tempElement.innerHTML = jsToInject;
        const appendScriptElement = function (index) {
            const scriptElement = document.createElement('script');
            const src = tempElement.children[index].getAttribute('src');
            if (src) {
                scriptElement.src = src;
            }
            scriptElement.onload = () => {
                index = index + 1;
                if (index < tempElement.children.length) {
                    appendScriptElement(index);
                }
            };
            scriptElement.type = 'text/javascript';
            body.appendChild(scriptElement);
        };
        if (tempElement.children.length) {
            appendScriptElement(0);
        }
    }
};
function AccountLinkingUnavailable(cause) {
    window.Alkami.AccountLinking.unavailable = true;
    document.dispatchEvent(new CustomEvent('Alkami.AccountLinking.unavailable', {
        detail: {
            unavailable: true,
            cause,
        },
    }));
}


/***/ })

/******/ });
//# sourceMappingURL=init.min.js.map